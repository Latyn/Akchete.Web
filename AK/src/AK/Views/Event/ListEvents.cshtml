@model IEnumerable<Ak.Models.Event>
@{
    ViewBag.Title = "Drag and Drop Event";
}
@section Scripts{
    <script type="text/javascript" src="~/wwwroot/lib/dragdrop/jquery.sortable.min.js"></script>
    <link href="~/css/Drag.css" rel="stylesheet" />
    @*http://web1.cs-computing.com/apps/demo/bootstrap/bootstrap_demo.nsf/dnd.xsp*@
}
<div class="row">

    <div class="col-md-6 col-md-offset-2">
        <div class="sideBySide">
            <div class="col-md-12 text-center">
                <h1>@ViewBag.Title</h1>
            </div>
            <div class="col-md-6">
                <ul class="source connected">
                    @foreach (var item in Model)
                    {
                        <li id="@item.Id">
                            @item.Name.ToString()
                        </li>
                    }
                </ul>
            </div>
            <div class="col-md-6">
                <ul class="target connected"></ul>
            </div>
        </div>
    </div>

    @*<div class="col-md-12">
            <ul>
                @foreach (var item in Model)
                {
                    <li>
                        @item.Name.ToString();
                    </li>
                    <li>
                        @item.Place.ToString();
                    </li>
                    <li>
                        @item.MusicGender.ToString();
                    </li>
                    <li>
                        @item.Latitude.ToString();
                    </li>
                    <li>
                        @item.Longitude.ToString();
                    </li>
                }
            </ul>
        </div>*@
</div>
<script type="text/javascript">
    window.onload = function () {
        $(".source, .target").sortable({
            connectWith: ".connected"
        });
    };
</script>